import React, { useState } from 'react'

function App() {
  const [count, setCount] = useState(0)

  return (
    <div>
      <button className="btn btn-primary">
        daisy UI Button
      </button>
    </div>
  )
}

export default App
